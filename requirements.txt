# =============================================================================
# Skillz Media Platform — All Python Dependencies
# =============================================================================
# Split files also available:
#   requirements-edge.txt   — Jetson edge device (player + detection)
#   requirements-server.txt — Central Hub, Local Hub, CMS
#   requirements-dev.txt    — Testing and development tools
# =============================================================================

# --- Edge device (Jetson) ---
# NOTE: PyGObject (gi) and GStreamer bindings MUST come from apt, not pip:
#   apt install python3-gi gir1.2-gstreamer-1.0 gir1.2-gst-plugins-base-1.0
PyYAML>=6.0
pyzmq>=25.0
requests>=2.31.0

# --- Server components (Central Hub, Local Hub, CMS) ---
Flask>=3.0.0
Flask-SQLAlchemy>=3.1.0
Flask-Migrate>=4.0.0
psycopg2-binary>=2.9.9
celery[redis]>=5.3.0
face-recognition>=1.3.0
faiss-cpu>=1.7.4
sendgrid>=6.10.0
twilio>=8.10.0

# --- Development and testing ---
pytest>=7.4.0
black>=23.0.0
pylint>=2.17.0
